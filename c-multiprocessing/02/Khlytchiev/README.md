`run x` чтобы запустить процесс от числа x

`list` вдает список процессов в формате:

[`индекс процесса`, `номер процесса`, `завершённость процесса`]

`exit` убивает все процессы и выходит из программы
